INCLUDE_DIRECTORIES(${CMAKE_CURRENT_SOURCE_DIR}/../MorseCodeConverter)

# TestDriver
SET(TESTEXEC TestConversion)

SET(CMAKE_RUNTIME_OUTPUT_DIRECTORY ${CMAKE_BINARY_DIR}/bin)

ADD_EXECUTABLE(${TESTEXEC} Test.cpp)
TARGET_LINK_LIBRARIES(${TESTEXEC} MorseCodeConverter)
